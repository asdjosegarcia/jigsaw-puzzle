import React from "react";


function Timer(){
    const timeStart=Date.now()
    const chronometer=Date.now()-timeStart

    // timeInterval = setInterval(showTime, 100)
    
    return(
        <>
        <div>--:--</div>
        {/* <p>wenasss</p> */}
        </>
    )
}
export default Timer

